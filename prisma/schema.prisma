generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:Matchola8228..@db.nerfryuccuxfucrjckdp.supabase.co:5432/postgres"
}

model PrayerLog {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  minutes   Int
  type      String
  createdAt DateTime @default(now()) @map("created_at")

  @@map("prayer_logs")
}