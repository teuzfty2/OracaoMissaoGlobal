generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PrayerLog {
  id        String   @id @default(cuid())
  minutes   Int
  type      String   @default("adicionado")
  userId    String
  createdAt DateTime @default(now())
}